import "./loader.css"

const Spinner = () => {
  return (
    <div className="loader_wrapper flex items-center justify-center w-full h-screen bg-slate-200">
      <div className="loader"></div>
    </div>
  )
}

export default Spinner
